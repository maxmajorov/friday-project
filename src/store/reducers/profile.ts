const initialState = {
  name: "Maxim",
  age: 32,
};

export const profileReducer = (state = initialState, action: any) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

// TYPES
