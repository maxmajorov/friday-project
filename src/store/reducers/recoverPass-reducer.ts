const initialState = {
  question: "What your favourite color?",
};

export const recoverPassReducer = (state = initialState, action: any) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

// TYPES
