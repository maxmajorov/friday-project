const initialState = {
  name: "Max",
  login: "major_101",
  age: 32,
  mail: "maxpredko@gmail.com",
};

export const registrationReducer = (state = initialState, action: any) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

// TYPES
